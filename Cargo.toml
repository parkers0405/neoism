[workspace]
members = [
    "sugarloaf",
    "teletypewriter",
    "corcovado",
    "copa",
    "neoism-proc-macros",
    "neoism-backend",
    "neoism-terminal-core",
    "neoism-frontend/wasm",
    "neoism-grapheme-width",
    "neoism-window",
    "neoism-notifier",
    "neoism-protocol",
    "neoism-product-docs",
    "neoism-workspace-index",
    "neoism-workspace-daemon",
    "neoism-terminal-pty",
    "neoism-frontend/shared",
    "neoism-agent/crates/neoism-agent-core",
    "neoism-agent/crates/neoism-agent-service-api",
    "neoism-agent/crates/neoism-agent-workspace-search-fff",
    "neoism-agent/crates/neoism-agent-neoism-adapter",
    "neoism-agent/crates/neoism-agent-plugin-api",
    "neoism-agent/crates/neoism-agent-builtins",
    "neoism-agent/crates/neoism-agent-server",
    "neoism-agent/crates/neoism-agent-cli",
    "neoism-frontend/desktop",
    "neoism-extensions",
    "neoism-sync",
    "neoism-rm-agent",
    "neoism-remarkable",
    "neoism-neoworld-core",
    "neoism-neoworld",
]
resolver = "2"

[workspace.package]
version = "0.7.70"
authors = ["Parker Settle", "Neoism contributors"]
edition = "2021"
license = "MIT"
keywords = ["graphics", "terminal", "application"]
# Minimal stable rust version (MSRV)
rust-version = "1.92.0"
repository = "https://github.com/parkers0405/neoism"
homepage = "https://github.com/parkers0405/neoism"
documentation = "https://github.com/parkers0405/neoism#readme"
readme = "README.md"

[workspace.dependencies]
# Note: https://doc.rust-lang.org/cargo/reference/specifying-dependencies.html#multiple-locations
# Sugarloaf example uses path when used locally, but uses
# version from crates.io when published.
teletypewriter = { path = "teletypewriter", version = "0.7.70" }
neoism-backend = { path = "neoism-backend", version = "0.7.70" }
neoism-terminal-core = { path = "neoism-terminal-core", version = "0.7.70" }
neoism-terminal-pty = { path = "neoism-terminal-pty", version = "0.7.70" }
neoism-terminal-wasm = { path = "neoism-frontend/wasm", version = "0.7.70" }
neoism-ui = { path = "neoism-frontend/shared", version = "0.7.70" }
neoism-window = { path = "neoism-window", version = "0.7.70", default-features = false }
neoism-notifier = { path = "neoism-notifier", version = "0.7.70" }
neoism-grapheme-width = { path = "neoism-grapheme-width", version = "0.7.70" }
sugarloaf = { path = "sugarloaf", version = "0.7.70" }
neoism-agent-core = { path = "neoism-agent/crates/neoism-agent-core", version = "0.7.70" }
neoism-agent-service-api = { path = "neoism-agent/crates/neoism-agent-service-api", version = "0.7.70" }
neoism-agent-workspace-search-fff = { path = "neoism-agent/crates/neoism-agent-workspace-search-fff", version = "0.7.70" }
neoism-agent-neoism-adapter = { path = "neoism-agent/crates/neoism-agent-neoism-adapter", version = "0.7.70" }
neoism-agent-plugin-api = { path = "neoism-agent/crates/neoism-agent-plugin-api", version = "0.7.70" }
neoism-agent-builtins = { path = "neoism-agent/crates/neoism-agent-builtins", version = "0.7.70" }
neoism-agent-server = { path = "neoism-agent/crates/neoism-agent-server", version = "0.7.70" }
neoism-extensions = { path = "neoism-extensions", version = "0.7.70" }
neoism-product-docs = { path = "neoism-product-docs", version = "0.7.70" }
neoism-sync = { path = "neoism-sync", version = "0.7.70" }
neoism-remarkable = { path = "neoism-remarkable", version = "0.7.70" }
neoism-neoworld-core = { path = "neoism-neoworld-core", version = "0.7.70" }
neoism-neoworld = { path = "neoism-neoworld", version = "0.7.70" }

# Own dependencies
anyhow = "1.0"
async-stream = "0.3"
copa = { path = "copa", default-features = true, version = "0.7.70" }
neoism-proc-macros = { path = "neoism-proc-macros", version = "0.7.70" }
corcovado = { path = "corcovado", version = "0.7.70" }
raw-window-handle = { version = "0.6.2", features = ["std"] }
parking_lot = { version = "0.12.5", features = [
    "nightly",
    "hardware-lock-elision",
] }
rustc-hash = "2.1.2"
smallvec = "1.15.1"

# unicode-width = "0.2.0"
unicode-width = { package = "unicode-width-16", version = "0.1.0" }
base64 = "0.22.1"
clap = { version = "4.5", features = ["derive", "env"] }
# `ripgrep` is fff's DEFAULT glob backend (pure-Rust globset). It must be
# on explicitly: with default-features off and neither `ripgrep` nor `zlob`
# enabled, fff-search 0.10.0 fails to compile — its constraints module calls
# glob helpers that only exist under one of those features. `ripgrep` avoids
# the native FFI the `zlob` SIMD backend would pull in (cleaner mac/win builds).
fff-search = { version = "0.10.1-nightly.31be224", default-features = false, features = [
    "ripgrep",
] }
dunce = "1.0.5"
globset = "0.4.19"
ignore = "0.4.29"
futures-core = "0.3"
# Keep libgit2's HTTPS + SSH transports, but compile its C stack and OpenSSL
# into release binaries. Without the vendored features, openssl-sys discovers
# Homebrew on macOS and bakes /opt/homebrew dylib paths into the executable.
git2 = { version = "0.20.2", features = [
    "vendored-libgit2",
    "vendored-openssl",
] }
image_rs = { package = "image", version = "0.25.10", default-features = false, features = [
    "gif",
    "jpeg",
    "ico",
    "png",
    "pnm",
    "webp",
    "bmp",
] }
regex = "1.12.3"
onig = { version = "6.5.1", default-features = false }
bytemuck = { version = "1.25.0", features = ["derive"] }
serde = { version = "1.0.228", features = ["derive"] }
wgpu = { version = "28.0.0", default-features = false, features = ["std", "parking_lot", "dx12", "metal", "gles", "vulkan", "wgsl"] }
libc = "0.2.185"
smol_str = "0.3.2"
futures = "0.3.31"
wasm-bindgen = "0.2.87"
wasm-bindgen-futures = "0.4.34"
wasm-bindgen-test = "0.3.42"
env_logger = "0.11.8"
console_log = "0.2.2"
bitflags = "2.9.0"
iovec = "0.1.4"
console_error_panic_hook = "0.1.7"
tracing = "0.1.41"
objc = { package = "objc-rs", version = "0.3.1" }
memmap2 = "0.9.5"
url = "2.5.4"
criterion = { version = "0.6.0", features = ["html_reports"] }
dashmap = "6.1.0"
flate2 = "1.0"
lazy_static = "1.5"
rand = "0.8"
serde_yaml = "0.9"
sha2 = "0.10"

# Workspace daemon stack
neoism-protocol = { path = "neoism-protocol", version = "0.7.70" }
neoism-workspace-index = { path = "neoism-workspace-index", version = "0.7.70" }
neoism-workspace-daemon = { path = "neoism-workspace-daemon", version = "0.7.70" }
tokio = { version = "1", features = ["full"] }
axum = { version = "0.7", features = ["ws"] }
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.6", features = ["cors", "trace"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde", "js"] }
thiserror = "1"
reqwest = { version = "0.12", default-features = false, features = [
    "rustls-tls",
    "stream",
    "json",
] }
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "sqlite"] }
tokio-stream = { version = "0.1", features = ["sync"] }
turso = "0.6"

[profile.release]
strip = "symbols"          # See split-debuginfo - allows us to drop the size by ~65%
split-debuginfo = "packed" # generates a separate *.dwp/*.dSYM so the binary can get stripped
debug = "full"             # No one needs an undebuggable release binary
codegen-units = 1
incremental = false
lto = true                 # reduces binary size by ~14%
panic = "abort"            # reduces binary size by ~50% in combination with -Zbuild-std-features=panic_immediate_abort

[profile.dev]
split-debuginfo = "unpacked"
lto = false
incremental = true
opt-level = 0
