---
name: feature-presence-avatars-and-agent-gui
description: "Pixel-plasma multiplayer presence avatars (editor/chrome/tree) + agent-pane copy-rendered-text, Back-to-sidebar, running dots, instant tree-click — all uncommitted 2026-07-28"
metadata: 
  node_type: memory
  type: feature
  originSessionId: 7f62545c-19db-444c-86e4-3b6a6574bcc7
  modified: 2026-07-28T22:15:15.589Z
---

**2026-07-28 batch, uncommitted (user tests before commit). Both targets compile clean.**

**Pixel-plasma presence avatars** — Rust port of synapse-ai-hub's `MemberOrb` (~/Github/synapse-ai-hub/apps/frontend/src/communications-shared/member-orb.tsx): FNV-1a(utf16) → xorshift32 RNG → 5-hue palette + 4-sine plasma per cell → HSL→RGBA → circle by skipping out-of-radius cells (round silhouette, no mask). Generator `neoism-frontend/shared/src/editor/crdt/presence_avatar.rs`, re-exported `neoism_ui::editor::crdt::{avatar_cells, AvatarProfile, AvatarCell}`. `profile.cells(ox,oy,size,t,|cell|…)`; still frame = `t=0.6`. **Seed = peer display_name** (host name like piss-desktop). Verified visually (rendered orbs to PNG, matches synapse). Wired 3 surfaces: editor caret name-flags (markdown remote_carets.rs + code render.rs), top-chrome face-pile cluster right of server menu (chrome_topbar.rs, cap4+"+N", hover tooltip, per-orb hit-test in handle_pointer_move, fed per-frame via cell_emit.rs `top_bar.set_peers(remote_presence.avatar_peers())`), file-tree rows (file_tree/render.rs, cap3). Shared helper `draw_presence_orb_clipped` in editor/markdown/render/draw.rs. Store enum API `RemotePresenceStore::avatar_peers()`/`avatar_peers_by_buffer()` + `PresenceAvatarPeer`. **Tree index is EVENT-DRIVEN, zero polling:** `FileTree.presence_index: HashMap<PathBuf,Vec<PresenceAvatarPeer>>` rebuilt only in `Screen::rebuild_file_tree_presence_index` called from `apply_daemon_crdt_message` guarded by `if presence_changed` (app/mod.rs:835) + server-switch reset. TODO: web/wasm chrome `set_peers` bridge (chrome_bridge_core.rs) not fed yet; desktop fully wired.

**Agent-pane GUI (3 features):** (1) **copy = rendered text, not raw md** — `draw_markdown_inline_line` (agent_pane/view/markdown.rs) now registers the selectable line via new `rendered_inline_text(line)` + width via `measure_markdown_inline_width` (per-segment rendered), not the raw string; drag-copy yields `bold and code` not `**bold** and \`code\``, and no phantom marker-width in the highlight. Code BLOCKS still copy source. One shared fix covers desktop+web. (2) **← Back to recent-chats while chatting** — `NeoismAgentSidePanel.show_home_override` (state/side_panel.rs) shows the recent-sessions list over the live chat; ← glyph (U+2190) app-font sticky header (view/side_panel/draw.rs), contextual label, non-destructive; switch_session resets override. (3) **green running dot** on active sessions (`session_entry_is_running` → BranchStatus::Active; desktop api.rs attaches runtime_status). Focus chain: Back joins focusable()/select_prev/next/only_back_focusable; click hit-test back_button_contains in desktop bridges/agent.rs. Web parity wired. 223 agent-pane tests pass.

**Instant tree click** — mouse-click open lagged vs keyboard Enter because the spring-drag design deferred open to mouse-RELEASE. Fix (screen/bridges/file_tree/mouse.rs + `Screen.file_tree_opened_on_press`): FILES open on PRESS (instant like Enter), FOLDERS still toggle on release (so folder-drag doesn't toggle); release `Click` is no-op'd when opened_on_press. File-drag-to-move still works (opens then moves — acceptable, VS Code parity).

**OPEN DESIGN DECISIONS (discussed, awaiting user):** joined-workspace notes + agent should be "the host's, live, via the one daemon connection" (like ssh). AGENT: already works via `/agent` reverse-proxy (`agent_server_override_for_current`, daemon_link.rs); cleanup = drop redundant `port+1` resolver (`agent_server_for_daemon_endpoint`). NOTES: can't share the host's VAULTS (would expose all host vaults in the selector + empty-create UX). DECISION = separate "personal vaults" (local-only, selector) from "project notes" that live IN the workspace dir (`<dir>/Notes/` vs `<dir>/.neoism/notes/` — user to pick) so they travel with the share, are scoped to the project, collaborative, and never sprawl vaults. Joined workspace = suppress vault selector, notes root in shared dir, optional private LOCAL vault overlay never shared. Not built yet. See [[bug_hosted_workspace_badge_and_join_adopt]], [[project_shared_notes_agent_scoping]], [[project_notes_overhaul]].
