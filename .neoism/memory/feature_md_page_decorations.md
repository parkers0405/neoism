---
name: feature-md-page-decorations
description: Notion/Thymer-style md page icon (emoji) + cover banner from frontmatter — core shipped; pickers + hover chips TODO
metadata: 
  node_type: memory
  type: project
  originSessionId: 61dc832e-208d-41c5-a0b4-dbc86d0b1599
---

2026-07-16: Markdown page decorations, driven by the properties block (`---` frontmatter):
- `icon: 🔥` → large emoji row above the title (native emoji in text stack); `cover: dusk` → edge-to-edge banner above icon/title; both scroll with content (title draws at `y+30+cover_h+icon_h-scroll_y` in shared virtualized surface.rs).
- Shared side (`render_data.rs`): pub `frontmatter_property/icon/cover` (quote-stripping); pane field `cover_overlay_rect: Option<[f32;4]>` — surface reserves the band + draws dim placeholder tint; HOST paints the real image (shared can't load files).
- Desktop bridge (`screen/bridges/markdown/render.rs`): `sync_markdown_cover_overlay` after each md pane render — resolves `cover:` value (absolute / has-`/` → relative to note dir / bare name → `<config>/covers/{name}{,.png,.jpg,.jpeg,.webp}`), decodes via image_rs once into `Screen.markdown_cover_cache` (None caches failures; re-decodes if sugarloaf evicts texture), registers GraphicData (image_id = path hash | 1<<30 — u32! sugarloaf image_data is keyed u32), pushes GraphicOverlay with aspect-fill center-crop + scroll clip via source_rect (normalized [x,y,w,h]).
- Write half for pickers ready: `MarkdownPane::set_frontmatter_property(key, value)` — replaces/inserts key line, creates the `---` block if missing, marks Complex pending edit + rebuilds blocks; dirty until save.
- Stock covers: 3 generated gradients (dusk/jade/ember, 1200x400 PNG) in `~/.config/neoism/covers/` AND repo `extra/covers/` (placeholder ship folder — user will provide the real set and say when to bundle via bootstrap).

**TODO next increment:** hover "Add icon"/"Add cover" chips above the title (register click rects like link_rects/copy_rects); cover picker with live previews (user wants theme-picker-style UI); emoji picker (user notes native emoji support exists; no picker widget yet — consider palette mode or inline_picker); bundling covers via bootstrap.rs on first run; web parity (cover overlay is desktop-only — web host needs its own image lay).

**Gotcha:** editing screen/mod.rs near struct decls — a bare `struct X` anchor can orphan its `#[derive]` line (my insert stole FileTreeResizeState's derive → E0507 move error).

**2026-07-16 title-cursor + overlay-leak round:** (1) virtual-title caret is now the REAL trail cursor — surface.rs sets pane.cursor_rect from the measured title prefix (block width = char under caret, "M" at EOL) instead of drawing a custom accent rect; body caret auto-suppressed (all setters guard cursor_rect.is_some, title set runs first). (2) Vim modes on the title line: pane.mode drives shape (cursor_shape()); begin_title_edit parks Normal block ON last char; input.rs title match gained a Normal arm (h/l/0/^/$/i/a/I/A/x, j=cancel, Esc in Insert→enter_normal+step-left, Esc in Normal=cancel, Backspace=move-left in Normal). (3) Caret chrome phase-through FIXED: set_cursor_rect_clipped now CLAMPS the rect to the pane clip (trail overlay draws it verbatim) so a half-scrolled caret shows its visible slice; rect_intersects_clip deleted. (4) Cover-overlay tab leak FIXED: render_markdown_panels clears image_overlays for contexts not in visible_nodes — switching md→terminal used to keep compositing the cover band over the splash (kitty terminal overlays unaffected: their sync self-clears).

**2026-07-16 covers + icon-sync:** user's 4 wallpapers processed into ~/.config/neoism/covers (magick, 1920w, banner pre-crops): last-supper.jpg (Leonardo, 4:1 center), creation.jpg (Michelangelo hands, native 3.2:1), disputa.jpg (Raphael, 4:1 strip at +220y — Christ enthroned row), ascii-hands.png (dot-art Adam hands, PNG kept for crisp dots). Cover band = pane-width x 170*font_scale, aspect-fill center-crop → pre-crop tall sources to ~4:1. Sidebar icon staleness FIXED: NotesSidebar::set_note_icon(path, icon) in-place update, called from the value-picker Enter accept in markdown/input.rs (entry was disk-walk stale until daemon flush). Notes splash resized to agent-heading size (16*scale, width-fit only on narrow panels). Per-folder template design (answered, not built): .template.md per folder (frontmatter defaults + {{title}}/{{date}} body, nearest-ancestor wins, hidden like project.toml); folder.toml only if folder BEHAVIOR config ever needed — rule: app config = JSONC, vault metadata = TOML, note content = md.

**2026-07-16 round 3:** (1) cover scroll FLIP root-caused: GraphicOverlay.source_rect is CORNERS [u0,v0,u1,v1] (shader mix(xy,zw)), cover code passed (origin,size) — identical at [0,0,1,1] rest, inverted sampler once scrolled (v1<v0) = squashed upside-down band; also fixed off-center crop at rest. (2) Sidebar emoji flash-then-revert: refresh_notes disk-walk stomped in-place icon → NotesSidebar.icon_overrides map (set_note_icon records; apply_icon_overrides in refresh_notes + set_entries_from_host; cleared on vault change). (3) Turso FTS5 startup warning DELETED (designed condition, not degradation); notes search LIKE path now tokenized all-terms AND (any order), 16/16 tests. (4) md `/` search: smartcase (lowercase query = insensitive, ascii-lower preserves byte offsets) + multi-word all-terms-anchored-at-first in BOTH incsearch::search_scan and motions::find_in_line (n/N agree). (5) chrome_topbar server status dot rect→rounded_rect circle. (6) gradient covers (ember/dusk/jade) deleted per user; TASKS.md still says cover: ember (placeholder tint until repicked). Animated covers: image_rs::open takes FIRST FRAME of gif/webp — Thymer-style motion = Ken Burns on source_rect (needs animation owner, redraw cost) or frame-decoded gif cache; both unbuilt, user shopping for assets (Adam Lister 8-bit paintings; waneella-style gif loops).

**2026-07-16 lines-pill wire:** status bar cur/total pill was dead on code panes because per-window-server cutover made ALL editors EditorBackend::Daemon, whose drain_winbar/drain_showcmd return None (winbar plane was Local-only). Fixed end-to-end: events.lua rio_winbar now sends nvim_buf_line_count(0) as arg 4 (local bridge.rs already parsed it); new EditorServerMessage::CursorLine{surface_id,line,total_lines} in protocol; daemon redraw.rs forwards rio_winbar -> CursorLine; desktop context_editor.rs handler sets editor_cursor_line/editor_total_lines (+ tab.rs redraw-events arm, daemon_layout.rs surface_id arm; wasm has wildcards). NOTE: showcmd + breadcrumb symbol still Local-only over the daemon link (follow-up candidates). Lua ships via include_str! -> prepare_rio_nvim_runtime, so a rebuild is enough.

**2026-07-16 cover orphan sweep (FINAL fix for screen-glued covers):** covers leaked whenever a pane stopped rendering via a path the per-frame clear missed (tab stashed to ANOTHER grid, content swapped md->terminal in same context, workspace switch) — overlay stayed screen-fixed over everything ("follows me as a bg"). Fix = ownership tracking in render_markdown_panels: Screen.markdown_image_overlay_ids (HashSet<usize>) records every rich_text_id fed this frame (cover sync + notebook sync); ids from last frame not re-touched get clear_image_overlays_for. Terminals untouched (kitty ids never owned by the sweep). This subsumes the earlier invisible-context clear.
