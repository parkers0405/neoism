[package]
name = "neoism-omarchy-installer"
version = "0.1.0"
edition = "2021"
publish = false

# Intentionally standalone: exercise the actual installer without desktop/GPU builds.
[workspace]

[dependencies]
serde_json = "1"
sha2 = "0.10"
libc = "0.2"
tempfile = "3"
