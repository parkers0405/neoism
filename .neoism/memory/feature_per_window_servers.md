---
name: feature-per-window-servers
description: "Per-window Server→Workspace WIP — my fixes (blank tree, stale tabs, stable window profiles, --workspace flag, docker join-test) + remaining gaps"
metadata: 
  node_type: memory
  type: project
  originSessionId: 61dc832e-208d-41c5-a0b4-dbc86d0b1599
---

2026-07-15/16: User's uncommitted WIP replaces the process-global daemon link with per-window server sessions (`window_sessions: HashMap<WindowId, WindowServerSession>`, `server_registry.rs`, palette Servers UI, spec in docs/architecture/server-connections.md). Model: server = daemon endpoint (place), workspace = daemon-owned dir on that place (root_dir), window = camera. Home-2 test server = second local daemon `ws://127.0.0.1:7979/session` (servers.json).

**Fixes I landed (all cargo-check clean):**
- Blank tree after server switch: reset cleared entries but root pathname survived so every already-there guard skipped repopulation; `switch_window_server` now forces `sync_file_tree_root_for_current_workspace()` post-attach (app/mod.rs).
- Old server's tabs surviving switch: `reset_server_owned_state` (daemon_layout.rs) now clears live `buffer_tabs` + `workspace_buffer_tabs`/`workspace_file_trees`/`workspace_roots`/`workspace_buf_enter_targets`/`workspace_editor_active_paths`/`file_tree_workspace` — WorkspaceKeys from different daemons can collide.
- Subscriptions dead across restarts: profile ids were per-run UUIDs; now stable ordinals `window-N` in creation order (`next_window_profile_id`), so servers.json `(profile, server)` keys re-match; main window = window-1.
- macOS compile break: 3 cfg(macos) sites re-routed from deleted router methods to `send_window_message` (`RequestCloseWindow`/`RequestOpenWindow`/`RequestOpenNativeTab`) — mirrors close.rs/user_event_dispatch patterns; unverifiable on Linux.
- Daemon `--workspace DIR` flag (repeatable): headless-host primitive, declares dir as workspace at startup via new pub `WorkspaceManager::declare_startup_workspace` (idempotent vs snapshot — verified live: same workspace id across restarts).
- docker-compose `join-test` service (profile-gated): builds daemon image, clones neoism from GitHub INSIDE container, serves via --workspace on host port 7980, NEOISM_REQUIRE_AUTH=0. `docker compose --profile join-test up -d --build`; genuinely-foreign fs join test. NOTE: `--profile join-test up` also starts the default cloud `daemon` service (9876, auth on).

**Round 2 fixes (2026-07-16, all cargo-check clean):**
- Join lands IN a workspace now: `needs_initial_workspace_adopt` flag on WindowServerSession (set on switch); when the new server's tree arrives and no subscription restored anything, auto-adopt `summaries.first()` (daemon sorts most-recent-first) via `open_or_adopt_daemon_workspace` → fresh grid + remote shell + fresh strip. Root cause of "tab row gone + file clicks reuse old nvim": switch never entered a workspace, old grid stayed current, `primary_editor_node_and_route()` reused old editor. Remote nvim ALREADY exists: `add_stacked_editor` prefers `create_daemon_editor_context` (daemon-side nvim) over local.
- `reset_server_owned_state` seeds `ensure_terminal_tab()` + scale on the fresh strip (empty strip hides the whole row).
- Modal input golden pass (shared widgets/modal.rs + route.rs): real caret (char-indexed `input_caret`, reset on open/focus/click), ArrowLeft/Right/Home/End/Delete, Ctrl+V paste (clipboard param already on `has_key_wait`), caret rendered at position (prefix measure) for both single input and form fields.
- Async server switch: dial on named thread → `PendingServerSwitch` over mpsc → `drain_server_switch_results` in pump completes swap; palette row shows Connecting/Online/Offline; no more 4s UI freeze on dead endpoints; `server_switch_inflight` guards double-clicks; window-closed-mid-dial dropped.
- User typo incident: entered 7890 for 7980 → "could not connect"; paste support exists to prevent exactly this.

**Round 3 (2026-07-16): "joined docker but it was like I was local" root cause** — `attach_daemon_client_with_runtime` unconditionally ran `ensure_daemon_sessions_for_all_routes()` + `sync_daemon_workspaces()`, MIRRORING the desktop's local workspaces INTO the joined server's tree (docker daemon ended up holding "Workspace 1 /home/parkersettle" + "Workspace 1 /", newer than the real workspace → auto-adopt picked the pushed local one → tree showed local fs). Also leaked 2x1 cwd-less placeholder PTYs into the host (shells at / in the container; attach rebinds routes at +2ms after Hello, visible in docker logs). Fix: both calls now gated on `link_is_home` — a guest attach declares nothing; adopt flows create sessions explicitly. Diagnosed via docker logs (Hello/spawn timeline) + `docker exec cat state.json` — container-side state is ground truth for what the desktop actually sent.

**Round 4 (2026-07-16): pollution persisted + nvim deaths** — (1) The attach-time mirror gate was NOT enough: `may_publish_workspace`'s None arm ("id unknown to daemon → publishable unless adopted") let `sync_daemon_workspaces`'s other callers + `set_daemon_workspace_root` re-publish local islands to a GUEST daemon ("Workspace 2, host parkersettle, root /", local tabs registered server-side → resurrected on every rejoin/restart; also caused tree-rooted-at-`/` after rejoin since junk was most-recent). Fix: None arm now requires `!link_is_peer` — unknown ids self-declare ONLY on the home link. (2) Local nvim processes DIED on switch: daemon's `NvimNamespaceGuard` (socket.rs) reaps every nvim session of a closed connection's namespace, and switching servers dropped the home connection. Fix: `WindowServerSession.parked_home` — switching away PARKS the home connection (socket stays open, nvims live); switching home reuses it (same namespace) via shared `complete_server_switch`; stale parked (non-Open) falls back to dial. Plus `ensure_daemon_session_for_route` now skips routes whose context already has a remote_pty binding (parked round trip wiped the cache maps but shells are alive — respawn buried them). Also: joined-daemon shells got no TERM ("TERM environment variable not set") + bare `sh` = no block integration (perpetual spinner) → daemon defaults TERM=xterm-256color + prefers bash; docker image now installs neovim (blank remote editors = missing binary) + bash + sets TERM/SHELL. Remote markdown reads now fetch via files plane ReadFile→FileContent (pane `apply_remote_source`/`mark_remote_loading`). **Diag workflow: `docker exec cat state.json` for junk workspaces/tabs = ground truth on what the client leaked.**

**Round 5 (2026-07-16): remote editors + tree scroll** — (1) Blank remote nvim = daemon spawn FAILING: debian bookworm nvim 0.7.2 chokes on ide init commands (`Vim(lua):vim/_meta.lua:597`) → "nvim spawn failed" retry storm (also froze OTHER workspaces' editors — shared editor plumbing starved; desktop-side isolation = follow-up). Fix: Dockerfile installs current nvim release tarball (/opt/nvim symlink), not apt. (2) MD "flash then black + dirty dot" = CRDT seed race: drain bound the buffer while the files-plane fetch was in flight, seeding the daemon doc EMPTY; its snapshot clobbered the fetched content. Fix: `MarkdownPane.remote_content_pending` — `drain_markdown_pane_crdt` skips pending panes; bind happens after `apply_remote_source`. Loading shows tree-style skeleton bars (both render paths), no red os-error. (3) Awful tree scrolling on joined workspaces: every Root re-list reply did `set_entries` (scroll → top) — the no-open-dirs path lacked the preserve logic the merged path had; plus duplicate in-flight Root listings (3s liveness recheck re-dispatching). Fix: preserve scroll+selection on Root re-lists, dedupe pending same-root requests in populate_from_dir. (4) ShareCurrentWorkspace/StopSharing = wave-5 tailnet visibility toggle (ShareWorkspace msg) — keep, not trash; dormant until discovery UX pass.

**Round 6 (2026-07-16): freeze mechanisms nailed (3 layers)** — (1) daemon NvimNamespaceGuard reaps nvims on connection close → fixed by parking; (2) parked home connection was never PUMPED (pump_daemon only drains window_sessions) → background islands' editors/PTYs looked frozen while visiting a guest; fixed: pump parked_home for pane-scoped frames ONLY (Editor+Pty applied; workspace/files/crdt/git dropped — they'd fight the guest state; return-home resync re-requests them); (3) daemon socket loop runs `nvim_sessions.get_or_spawn().await` INLINE (socket.rs ~1091) → head-of-line blocks ALL frames on that connection during spawns — architectural, minor with fast spawns, follow-up = move spawn off the hot loop behind an mpsc sink writer. Docker image: nvim pinned v0.12.3 tarball; builder capped CARGO_BUILD_JOBS=4 (uncapped in-container release build saturated every host core — the "what is killing my CPU" incident; any source edit invalidates COPY . . → full rebuild). Stale server-side CRDT docs from pre-fix sessions clobber even fixed clients → container restart clears hub (empty-doc was seeded by the old client; fixed client can't recreate it).

**Still open (round 4):** status-bar dir pill flickers during island back-and-forth (pane-heuristic root vs daemon root fighting, settles); picker "selected" state is the daemon's server-global active pointer, not what THIS window views — close-island-then-reclick confusion (grid_index_for_workspace_id may also go stale after island close); guest→guest switches still drop the guest connection (its nvims die — only HOME is parked).

**Why:** tab/tree caches are server-owned state; anything keyed by WorkspaceKey must be wiped on server switch. Profile identity must be deterministic or all persisted per-window prefs are write-only. A server switch must END in a workspace adopt — the pointer-switch fallback leaves the old server's grid live. And home-daemon mirroring must NEVER run against a joined server — gate it at the ownership guard (`may_publish_workspace`), not per call site.

**Still open:** remote agent-endpoint routing on switch (reset points panes at local `neoism_agent_server()`; per-workspace override exists via `agent_server_override_for_current` on adopt), `observe_rehome` untested + only follows first re-homed workspace per push, heavyweight health probes (thread+runtime per saved server, palette-only), corrupt servers.json → silent temp-dir fallback, deferred UX (context menus, tailnet Discovered section, failure details), editor/terminal PANES still showing old-server content after switch (only strips/caches cleared). Product direction agreed: join (shared checkout, presence) vs checkout (git clone to own machine, "clone repo → dir" workspace creation) — checkout flow not built. Related: [[project-cross-laptop-status]], [[project-workspace-root-model]].

**2026-07-16 joined-workspace finder (Alt+S) FIXED:** finder searched the LOCAL fs at the remote root (doesn't exist) -> always "no files". Desktop now rides the daemon's EXISTING search plane (wave-7 web parity, search.rs/search_handler in daemon): SearchClientMessage/SearchServerMessage variants added to desktop daemon_client (OutboundServiceMessage::Search + ServiceClientMessage::Search + "SearchReply" inbound arm + ack_pending/request_id arms), Screen::apply_daemon_search_message (bridges/finder.rs) -> finder.handle_service_reply (shared finder already daemon-aware from web). NativeSearchService gained RemoteSearchRoute (root+DaemonClientHandle+runtime, mirrors RemoteFiles): remote_dispatch intercepts collect_files/search_files/search_grep/collect_git_changes with IoError::Pending; installed/cleared in sync_file_tree_remote_mode alongside set_remote_files. Daemon-side resolve_cwd accepts absolute-under-workspace_root() (env NEOISM_WORKSPACE_ROOT or daemon cwd) — multi-workspace daemons whose extra workspace lives OUTSIDE that root would reject (same gap as web; follow-up). Lines pill in docker = stale image (needs CursorLine daemon) — image rebuild required. git_repo_root stays local-only (sync Option API, no Pending contract).

**2026-07-16 md save "could not write file:///home..." FIXED (view/link desync trap):** switch_window_server refuses while has_unsaved_server_buffers — but that counted EVERY modified tab incl. client-owned MARKDOWN panes, so a dirty local note blocked the connection from following a workspace click back home while the VIEW still flipped -> window kept the docker link -> SaveBuffer wrote /home/... inside the container -> "save failed: No such file or directory" AND user couldn't switch (deadlock: can't save on wrong daemon, can't switch with unsaved buffer). Fix 1: gate now uses new buffer_tabs::has_modified_server_tabs (only BufferTabTarget::File = nvim server-side buffers; md panes ride along on switches). Fix 2: save_current_markdown_via_daemon wrong-daemon guard — peer link + current workspace NOT remote-joined -> return false (caller does the plain local write). Remember: window CRDT/save traffic all rides window_sessions[window].connection — ANY view/link desync misroutes saves.

**2026-07-16 stale workspace-tab menu removed:** Share Workspace / Stop Sharing / Send to Docker Sandbox / Send to Cloud deleted from the Island-tab right-click menu (pre-server iteration; joining a server IS sharing now). Removed: WorkspaceChromeActionKind/Spec/WORKSPACE_CHROME_ACTIONS + both fns (shared context_menu.rs), 4 WorkspaceContextAction variants + desktop arms + menu-builder loop, wasm lib.rs exports (web TS callers are optional-chained, safe), dead share/stop_sharing_workspace_at_index. KEPT: WorkspaceChromeVisibility (palette + wasm file_tree_workspace badges), protocol ShareWorkspace/SendWorkspaceTo* messages + palette actions (wave-6 promote plane untouched). Also: desktop tokio-tungstenite has rustls-tls-webpki-roots -> wss:// joins (ngrok/CF tunnel) work; LAN = ws://192.168.x.x:7980/session. Self-host docker one-liner = follow-up (join-test mounts nothing; generic `server` compose service needs uid-mapping story for mounted workspace writes).

**2026-07-16 self-host docker (SHIPPED + verified):** compose profile `server` — NEOISM_SERVE_DIR=<proj> NEOISM_SERVE_TOKEN=x docker compose --profile server up -d server → ws://<ip>:7981/session. Root entrypoint fixes ownership then setpriv-drops to the PROJECT DIR OWNER's uid/gid (stat -c %u on the mount; NEVER chown -R into the project mount — nested bind under $home, chown is non-recursive on the state dirs only); state in named volume neoism-serve-state chowned at boot; token env auto-sets NEOISM_REQUIRE_AUTH=1 (token alone does NOT gate Hello — log 'require_auth=false' trap). Verified live: health OK, daemon uid 1000, require_auth=true, workspace adopted. .github/workflows/docker-publish.yml pushes ghcr.io/<owner>/neoism-daemon on v* tags + dispatch (buildx, gha cache). Domain vision (discussed, unbuilt): relay/rendezvous service on a bought domain — daemons dial OUT and register a subdomain, joiners hit wss://name.domain/session, relay pipes frames; kills port-forwarding + TLS setup for users (VS Code tunnels model).

**2026-07-16 agent-on-server v1 (WRITTEN, NOT COMPILED — user embargo: no builds until tomorrow; MUST cargo check neoism + daemon before next build):** joined agent panes now target the SERVER's co-hosted agent. Desktop: api.rs agent_server_for_daemon_endpoint (ws(s)://host:port/session -> http(s)://host:port+1 convention, None for unix/parse-fail), exported in agent/mod.rs; complete_server_switch (app/mod.rs, after attach_session_to_window) re-points panes via set_agent_server_for_window when !session.is_home (reset_server_owned_state already restores local on home-return). Container: Dockerfile builds+ships neoism-agent (bin from neoism-agent-cli crate); server profile publishes ${NEOISM_SERVE_AGENT_PORT:-7982}:4096, starts agent backgrounded under same setpriv identity with HOME=state volume (auth.json persists), OPENAI/ANTHROPIC key passthrough; daemon stays foreground lifeline (agent death = errors until container restart). Compose yaml parse-validated only. ALSO uncommitted: handshake.rs daemon_token_matches (Hello now accepts NEOISM_DAEMON_TOKEN — was 'invalid pairing token' for GUI token field since store-only verify; found live from Mac join). QUEUED for tomorrow: Create Server / Join Server two-flow UI (create = auto docker/native host + auto-join; join = URL + password), add-server modal polish (arrow key text->buttons focus), rename token field label to "Password". Multiplayer topology lesson: host must ALSO join its own server (two daemons = two CRDT hubs; bind mount converges only on save).

**2026-07-16 night (ALL UNCOMPILED — same embargo):** (1) REMOTE NOTES = workspace-scoped `Notes/` folder on the server: open_neoism_notes_sidebar + grid_workspace swap set ("Workspace notes", remote_root/Notes) + request_remote_notes_listing (daemon_sync.rs: WalkTree "Notes" depth 6, pending_remote_notes_listing HashSet on Screen); apply_daemon_files_message TreeListing arm -> set_entries_from_host (abs = notes_root.join(entry.path)), Error-while-listing -> empty panel (missing dir OK); create_notes_file remote branch -> send_remote_files_op CreateFile (dir rel to remote root); FileCreated under notes root -> open_path_in_markdown + re-list (else editor as before); notes_sidebar_create_target remote -> workspace_path; refresh_notes_sidebar_if_visible remote -> re-request (local walk would blank it); vault menu remote -> notification (personal vaults never leak). (2) CODE-FILE multiplayer asymmetry root cause: no autoread/checktime anywhere — per-client nvims only converge via disk, and daemon-side nvims get no focus events; fix in events.lua: vim.o.autoread + checktime on FocusGained/BufEnter/CursorHold + 2s defer_fn poll (skips insert mode; silent!; modified buffers keep last-writer). Lua parse-checked. VERIFY TOMORROW: cargo check -p neoism-ui -p neoism -p neoism-workspace-daemon before any build; risk spots: daemon_sync.rs match arm order (notes_listing arms before own_op arms), PathBuf paths, is_some_and on Option<PathBuf>, agent/mod.rs re-export. (3) Q&A settled: shared agent defaults = commit .neoism/neoism.json in the repo (upward discovery, synapse-tenant pattern) or config.json in server state volume; project picking = NEOISM_SERVE_DIR per container (multi-workspace picker = part of tomorrow's Create/Join Server UI).

**2026-07-16 late night — CREATE/JOIN SERVER UI (UNCOMPILED, same embargo; user was rightly pissed it got deferred — build what's asked WHEN asked):** palette Servers mode now shows "+ Create server" + "+ Join server" (renamed from Add): PaletteRow::ServerCreate (modes.rs variant/label/action + 7 grouped arms in update.rs via script + row push + render.rs plus-style), PaletteAction::CreateServer (actions.rs + grouped list + wasm support.rs label + palette.rs arm). open_create_server_prompt = form (create_dir prefilled from workspace_root_for_new_shell, name, password) -> ServerFormSubmit discriminates on create_dir -> create_and_join_local_server (palette.rs): ~ expand, dir validate, free-port scan 9877..9899, spawn sibling neoism-workspace-daemon (--addr 0.0.0.0:port --state-dir config/hosted-servers/<port> --workspace dir; password => NEOISM_DAEMON_TOKEN + REQUIRE_AUTH=1 — pairs with the handshake fix in the SAME build) + sibling neoism-agent serve on port+1 (agent-follows-server convention), request_server_add(ws://127.0.0.1:port/session) reuses dial-with-retry, LAN-ip share toast via UDP-connect trick. Token fields relabeled "Password" (join + edit forms). MODAL POLISH: modal.rs form_focus_move(down) — ArrowDown walks fields top->bottom then button row, ArrowUp reverses (skips hidden/empty-label fields like Tab; form_focus==len = buttons zone, input accessors None-safe there); route.rs arrow arms try form_focus_move first; button render highlights selected (modal.rs:1513) so the walk is visible. form_buttons_focused() helper exposed for renderer use.

**2026-07-16 latest (UNCOMPILED, embargo):** server modal round 2 per user: (1) ONE palette row "+ Add server" again (ServerCreate row push removed; variant/action kept) -> modal titled "Add server" with SEGMENTED SLIDER top-center [Join server | Create server]: modal.rs form_tabs/form_active_tab/form_tab_rects (cleared in open_form+close; set via set_form_tabs AFTER open_form since ModalFormSpec initializers stay untouched), pills drawn in the TITLE ROW (no layout shift; rects recorded logical), form_tab_hit(x,y); handle_modal_click checks tab hit FIRST -> reopens open_add_server_prompt/open_create_server_prompt (both pub(crate) now). (2) Submit no longer dumps to nowhere: ServerFormSubmit arms call request_server_manager() after add/edit/create so the Servers list reopens with status. (3) OVERLAY SOLIDITY: mouse.rs — command_palette.is_enabled() intercept right after the modal intercept: row click acts, anything else closes palette; NEVER falls through to panes (this was the 'click through the modal' complaint — the palette had no outside-click guard; the modal itself was already solid at mouse.rs:76). VERIFY: does handle_palette_click return false on palette-card padding clicks (would close on own padding — check tomorrow).

**+ Ctrl+Tab flips the server modal slider** (route.rs arm ABOVE plain-Tab field walk; modal.rs form_tab_count/form_active_tab getters). Docker-in-Create decision: NOT v1 — later as 'Isolate in Docker' checkbox with preflight (security angle: native hosting gives guests shells on the real machine; docker jails them).

**+ modal caret/highlight polish (uncompiled):** form + single-input carets now THICK translucent block (INPUT_FONT_SIZE*0.55 wide, accent a=0.55, no +2px offset; INPUT_CARET_WIDTH const deleted); button selection highlight in FORM modals only renders when form_buttons_focused() (pre-lit Enter while typing read as false focus; plain button-list modals unchanged).
