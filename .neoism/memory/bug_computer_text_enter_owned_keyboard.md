---
name: "Text then Enter fails: owned native keyboard and full-layout overlays"
description: "Text→Enter: stop inheriting virtual text-only baselines; full-layout overlays + same-connection native shortcuts; ACK restore AND destroy, exact parser/native sequence tests"
type: "bug"
scope: "project"
origin: "live failure follow-up, compositor source inspection and native/parser regressions"
created: "2026-09-12"
updated: "2026-09-12"
---

Supersedes restoration claim in bug_computer_live_parser_poison_lua_focus_settle.md. User live URL text succeeded then Return had no unmodified key. Native XKB test reproduces that exact failure on old URL-only map (without newline there is no Return). Source-verified holes: snapshot accepted whatever map seat advertised as original (possibly prior temporary virtual map); cleanup ACKed restoration but only flushed removal; Action::Key created Enigo/new VK before a different client resolved shortcuts. Installed Hyprland0.56.2 efb5099 InputManager creates/selects new keyboards, updates seat map only for active device, and selects last remaining keyboard on destruction. Seat map is active-device map, not guaranteed physical. No historical wire trace identifies which client supplied the bad map in the user's run. Permitted current passive probe sees normal Return36/Control37/Shift50/Meta133/a38/l46; this does not establish prior transient ordering.

Linux Text AND Key now route through owned native keyboard transactions in linux_text.rs BEFORE Enigo construction. One Wayland connection observes map/group before creating VK, resolves codes in the exact published map, injects, restores and removes. shortcuts::read_map retains FD offset-zero native decoder; resolve_in_map uses exact layout. No Return exception/US fallback. Enigo still handles pointer and non-Linux paths.

Text extends a FULL desktop map with unused high codes, NEOISM_LITERAL one-level NoAction symbols; original keys/types/layouts/modifier maps remain, so Return/Control/Shift/etc retain codes even while overlay is advertised. Native canonical symbols section marker neoism_overlay. Baseline validation rejects own neoism_text/neoism_overlay and older unnamed all-KNNN maps, bounds code range. Stale own map yields error BEFORE input asking physical keyboard reselect; never guess physical layout or inject repair keys.

Production transaction has small Wire(map/key/mods/sync/destroy) seam. On success/cancel/error/recoverable panic: release all held codes, publish ORIGINAL MAP FIRST, reset own mods/group, ACK restoration, destroy VK, ACK destruction. Steps independently attempted; ACK errors propagate/merge with original error. Nonblocking Drop fallback. Same connection observes through removal and rejects remaining temporary marker. Failed Linux keyboard calls invalidate frame. Global permission/revocation/serialization/batch recovery-image/target safeguards unchanged. Unpreemptible native calls and panic=abort remain limitations.

New linux_keyboard_tests.rs queued-wire fixture applies only on sync; REAL native XKB parsing/keysym/modifier state + production transaction. Before destroy, asserts original already observed and mods neutral (fallback cannot mask missing restore). URL→Enter→Ctrl+L→text→Enter on us/de/fr; overlay preserves normal chord codes;512Unicode; named+unnamed stale baseline rejection; text and Control-down cancellation; partial key error/panic; missing restore ACK still removes/reports error. scripts/check-computer-keymap.py includes unmodified actual Enigo keymap2 source, production shortcuts, exact logical-key parser extraction; EVERY published/restored sequence map passes actual Enigo FD loader + native reserialization. Harness23passed/1passive-probe-ignored; normal computer suite37passed/1ignored; explicitly allowed passive probe1passed; integrated desktop/server check and exact-source macOSARM64/WindowsMSVC checks pass; MCP18/model14 pass; diffcheck clean. No typing/click/focus/capture; native runtime sequence still needs authorized live smoke test. Docs updated; preserve unrelated edits.
