---
name: "TS GUI automatic native server registry and shared chats"
description: "Automatic local GUI registry bootstrap replaces manual bridge: HttpOnly launch session + guarded Vite proxy, shared native file, no flags/operator JS tokens; 673 GUI tests + tsc/Rust check; live4096 still404"
type: "feature"
scope: "project"
origin: "neoism-agent"
created: "2026-09-14"
updated: "2026-09-14"
---

LATEST: user REJECTED both plumbing Servers UI and then manual Connect local registry opt-in bridge. Current implementation is native-style list + AUTOMATIC local GUI launch session, no registry flags/forms/address/token JS state. Older memory statements about /operator/servers and NEOISM_SERVER_REGISTRY_API/TOKEN are superseded; that daemon route/module removed entirely.
Native registry implementation now lives neoism-agent-service-api/src/server_registry.rs (lightweight shared file IO helper); neoism-backend/src/server_registry.rs and desktop module re-export. Same servers.json/server-credentials.json in native config directory; service helper registry_directory mirrors platform paths/NEOISM_CONFIG_HOME. Existing lock/reload/atomic writes, row CAS, credential preservation name edits/clearing endpoint changes stay. Desktop picker reloads on open/edit/select. Browser polls focus/5s. No browser-only saved list or silent importing old experimental storage.
AUTOMATIC BOOTSTRAP: neoism-agent-server/src/local_gui.rs + gui.rs. Loopback-only served GUI context. Direct user navigation (Fetch Metadata none/navigate/document, actual loopback ConnectInfo peer + exact loopback Host/port, no forwarded headers) mints HttpOnly SameSite=Strict session scoped Path=/__neoism/gui; session max12h. Registry /__neoism/gui/servers GET/POST/DELETE requires session, same-origin Fetch Metadata, X-Neoism-Gui:1, no bearer Authorization. No CORS allowance, frame DENY/CSP on GUI. Nonloopback listener and hosted auth config => no registry context. Public assets carry NO credential. If NEOISM_AGENT_TOKEN exists, bootstrap verifies the existing local API key; navigation alone does not bypass it.
CLI web_launcher.rs now POSTs /__neoism/gui/launch with existing local API auth (env token sent ONLY to literal loopback origins), receives one-use 60sec ticket path. OS browser opens ticket, redeems to HttpOnly GUI session then redirects clean /. Never API/operator bearer in URL. --no-open ticket expires; rerun web. Old404/405 gracefully plain GUI fallback. Fresh server's ready future does launch request while serving future continues polled (avoid paused-server deadlock). Actual axum serve now into_make_service_with_connect_info for GUI boundary.
Vite scripts/localRegistryProxy.ts wired via vite.config. Browser local-navigation cookie gates same-origin private proxy. Node obtains upstream GUI session automatically, stores upstream cookie/API auth only server-side. Upstream loopback-only default4096 or existing NEOISM_AGENT_URL/VITE_NEOISM_AGENT_URL; existing NEOISM_AGENT_TOKEN reused server-side if configured. No redirect following, remote/guest/CSRF/iframe/forwarded-host access denied. JS NativeServerRegistry uses fixed relative path + credentials same-origin, no token/address configuration. Old/agent-only/unauthorized contexts get short Saved servers unavailable in this session note and Local/current + Connect without saving, never fake synced empty list. Contextual REMOTE daemon pairing remains separate and unchanged, workspace-scoped chats/SSE and guest provider gating preserved.
VERIFIED full GUI 68 files /673 tests pass incl identity/avatar/bubbles, tsc --noEmit passes, final focused27 tests pass after cookie path hardening. cargo check -p neoism-agent -p neoism-agent-service-api -p neoism-agent-server -p neoism-workspace-daemon -p neoism --tests passes incl automatic local launch/shared-registry/CAS/guest/one-use/configured-auth tests COMPILED ONLY (user says cargo check only). No release build/backend restart/browser tools. Read-only live HTTP probes: local Agent127.0.0.1:4096 rootHEAD404 and /__neoism/gui/servers GET404; Vite5174 not listening; daemon7878 root200 oldweb HTML. Thus SOURCE done but current Agent endpoint not updated. Need updated Agent CLI/server + GUI assets or Vite config + desktop shared lock before live integration; do not claim running backend works or restart without user authorization. README updated automatic setup/security/deployment constraints. Identity files untouched except targeted Settings/controller wiring from prior increments.
