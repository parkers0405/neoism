---
name: project-notes-overhaul
description: "Notes program — turso migration, legacy .neoism purge, per-vault graph (done), sidebar redesign pending"
metadata: 
  node_type: memory
  type: project
  originSessionId: 61dc832e-208d-41c5-a0b4-dbc86d0b1599
  modified: 2026-07-21T04:21:06.931Z
---

2026-07-16 notes overhaul (user directive): ONE notes model = Vaults (~/Neoism/Vaults/{vault}); migrate notes.sqlite (sqlx) → turso (agent-server pattern); graph per-vault; then Alt+N sidebar redesign.

**Recon (2 explorer reports, key facts):** storage = neoism-workspace-index, sqlx 0.8, per-vault db at ~/.cache/neoism/vaults/<cache_key(vault)>/notes.sqlite (graph_db.rs open_pool 739-754, SCHEMA 786-920 v4; blocks_fts = FTS5, manually maintained, LIKE fallback already in query.rs search 561-570). Turso reference = agent-server state.rs (Db enum, DbRow, turso_busy_retry, fts_enabled gate, vector32/vector_distance_cos need NO feature flags — semantic search door). Sidebar = shared notes_sidebar.rs (1499 ln) + desktop bridges (sidebar.rs/notes_menus.rs/vault_ops.rs/note_index.rs); lists via fs walk, never sqlite; viewed vault = NotesSidebar.workspace_name/path; persisted selection = per-code-project .neoism/workspace.toml notes.workspace. Graph = open_neoism_graph_view (notes_create.rs) → NoteGraph::graph; data ALREADY per-vault (db per vault), but view resolved vault from CODE ROOT.

**DONE by me:** (1) per-vault graph — new `vault_notes_workspace(name)` in workspace-index config.rs (+ lib.rs export); open_neoism_graph_view resolves from notes_sidebar.workspace_path() first (Vaults-root pseudo-vault falls back to code-root resolution). (2) Dead code deleted: neodraw/graph.rs (graph_scene static path, superseded by GraphSim), open_notes_vault_actions_menu, create_current_neoism_drawing. All cargo-check clean.

**IN FLIGHT (2 background agents):** (M) turso migration of graph_db.rs/query.rs — Db enum, env NEOISM_NOTES_DB_BACKEND (default TURSO), notes.turso.db separate file, fts_enabled gate → LIKE fallback, public API unchanged; (L) legacy purge — WorkspaceAction::{InitNeoismWorkspace,ReindexNeoismNotes} + daemon arms + shell_ops init/reindex + palette action variants + CLI `neoism init`/`notes init` + wasm labels; create_neoism_note reworked off load_workspace gate. File ownership was explicitly divided (M: graph_db/query/index-Cargo.toml; L: protocol/daemon/palette/note_index/main.rs/wasm).

**LANDED (both agents + my closeout, all verified):** turso migration complete — NoteDb enum, NEOISM_NOTES_DB_BACKEND (default turso → notes.turso.db beside notes.sqlite, rebuilds from markdown, never touches old file), fts_enabled gate → LIKE on turso, 16/16 tests both backends; delete_note_rows now explicit child deletes (turso FK enforcement not guaranteed). Legacy purge complete — WorkspaceAction::{Init,Reindex}NeoismNotes deleted end-to-end (protocol+daemon+palette+wasm+CLI `neoism init`+web TS senders/types), daemon create_neoism_note vault-first (24/24 daemon workspace tests), NoteGraph::init deleted, NoteGraph::open now vault-first (linked project.toml → root workspace.toml → Default vault; never requires .neoism), agent-server notes tool "init" = open. My closeout fixed: wasm PaletteWorkspaceEntry.current + server-variant label arms; value picker moved to OVERLAY pipeline (was in-pane layer → see-through card). Pre-existing broken (not chased, noted by agents): neoism-protocol crdt.rs:313 missing `rainbow` field blocks its lib tests; daemon integration tests (SessionCwd non-exhaustive); 3 neoism-ui lib-test errors.

**TODO after agents land:** verify both + run tests; retarget NoteGraph::open/init off .neoism (query.rs 130-151 — deliberately left out of both agents); decide fate of init_workspace/.neoism/workspace.toml layer (still used by vault linking via load_workspace; vault linking's real model is vault project.toml — could retire .neoism marker entirely later); Alt+N sidebar redesign (map in explorer report: header "Neoism Notes" + graph/⋮ icons, fs-walk rows, footer vault switcher, springs copied from file_tree, forked draw_frame_top, no skeleton); semantic note search via turso vector32 (door open, model on message_embeddings); web parity for notes plane.

**Sidebar redesign round (2026-07-16, all compiled):** Alt+N header = NEOISM per-letter wordmark (reused `agent_pane::view::wordmark::render_wordmark` + NeoismWordmarkState; renderer.notes_sidebar_mouse pushed from agent bridge; is_animating drives hover settle; render() gained mouse+now_seconds params — web passes None/0.0). Header graph/⋮ icons DELETED (NotesHeaderAction gone); footer = vault selector + settings gear (settings_rect/settings_selected, ArrowRight/Left walks selector↔gear, Enter/click → "Notes" context menu: Visualize Graph / Add… → existing create menu as submenu via new ModalAction::{NotesOpenGraph,NotesOpenCreateMenu} + lifecycle.rs policy tags + modal.rs executor arms). project.toml hidden at vault root. Vault resolution: NOT-linked dirs now ALWAYS Default vault (active_notes_workspace_for_root drops the .neoism/workspace.toml middle step). **BLACK-PARAGRAPH TRULY SOLVED:** md virtual surface culling window is offset from drawn positions by viewport.y (pad_top); md overrides overscan_px=260 (surface.rs 2 sites, now 700) — cover+icon+title pad_top >300px exceeded 260 → nodes culled while on screen; ALSO cover z_index must be 1/AboveText (BelowText renders under the pane bg rect → picture blacks out); content can't overlap the band by construction. **#6 DONE (2026-07-16):** per-workspace notes panels — `workspace_notes_sidebars: HashMap<WorkspaceKey, NotesSidebar>` + `notes_sidebar_workspace` swap in load_current_workspace_chrome (mirrors tree swap; visibility/focus/width/scale = window chrome, vault/entries/selection = workspace state); fresh LOCAL panels auto-resolve their vault via new `assign_local_vault_to_notes_sidebar`; REMOTE-joined workspaces get an empty "Host notes" panel (guard in open_neoism_notes_sidebar via current_workspace_is_remote_joined — personal vault can no longer leak into docker islands; daemon-listed remote vaults = follow-up); reset_server_owned_state clears the map + fresh live panel (NotesSidebar::set_width added for chrome transfer). Press Start 2P LANDED (agent): sugarloaf/src/font/resources/PressStart2P/ (TTF+OFL, NOTICE line), FONT_PRESS_START_2P in constants.rs, registered LAST in FontLibraryData::load (bottom of fallback cascade), `primitives/pixel_font.rs::pixel_font_id` (family lookup + ensure_static_font wasm fallback); applied to agent side-panel section headings (drawn size −3px vs layout size — pixel face ~1.7x wider) + cyan date-group headers. **Sidebar polish round 2 (2026-07-16):** splash header now the TEXT "Notes" in Press Start 2P (wordmark image dropped; per-letter hover lift + shimmer kept via NeoismWordmarkState + WordmarkState trait, word fitted to full panel width, cap 44px·scale); Alt+Arrow = panel-level nav only (close_focus.rs no longer calls move_horizontal_focus — plain arrows do the selector↔gear walk in sidebar.rs); focused gear now parks the block cursor just left of the settings button (selected_cursor_rect set in the settings branch — it used to vanish).

**Also this session (md):** PROPERTIES label removed; icon/cover LSP-style value picker (line = searchbar; Up/Down/Tab/Enter; emoji const EMOJI_CHOICES ~130 in pane.rs; covers from ~/.config/neoism/covers via pane.available_covers set in open_path_in_markdown); md skeleton = grace-delayed (80ms) fade, full viewport; status dir pill debounced 250ms (CWD_LABEL_SETTLE_MS) — flicker fix; black-paragraph bug NOT solved: culling layers eliminated, occlusion-rect theory prime (draw_if_visible drops rows intersecting occlusions while bgs paint) — tripwire shipped: NEOISM_MD_OCCLUSION_LOG=1 logs the active rect set; need user screenshot + cursor position + block type on next repro. Sidebar note icons (frontmatter icon in Alt+N rows) + editable virtual title (rename file) still TODO.

**2026-07-20 Memory layout flip (agent, blind, UNVERIFIED by cargo):** default vault memory moved `Personal/Memory` → `Memory/Personal` (matches project vaults' top-level `Memory/`). ONE code site: agent-server `mcp_memory.rs` (USER_MEMORY_DIR + LEGACY_USER_MEMORY_DIR; `migrate_legacy_user_memory` renames-only on first mutating touch via ensure_memory_root — memory.init/memory.write; reads via roots_for_scope fall back to legacy dir until migrated, never mutate) + `agent_native.rs` system-prompt text. Sidebar/file-tree/welcome-seed render folders generically — untouched. Empty `Personal/Memory` shell remains after migration by design. Stale semantic-embedding rows filtered by root_key. Live disk still legacy layout until first in-app memory write.

**2026-07-16 empty-state + polish:** Alt+N empty vault now draws a "+ New note" button (empty_create_rect + NotesSidebarHit::CreateFirstNote); dead `a`-on-empty fixed via notes_sidebar_create_target (target_dir else assign_local_vault_to_notes_sidebar retry; remote-joined -> None). MD page icon now renders INLINE "{emoji} Title" (icon_h band deleted from pad_top; title_x advances past measured icon; title-edit caret offset follows). chrome_topbar server rack icon: right closing stroke added (was open-bracket look).
