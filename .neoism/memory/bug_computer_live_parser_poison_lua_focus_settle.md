---
name: "Computer live failures: exact Enigo parser, poison recovery, Lua focus, screenshot settling"
description: "Verified Enigo unnamed-section FD-parser panic + poisoned busy lock; Lua Hyprland focus dialect; original keymap restore and bounded visual settling"
type: "bug"
scope: "project"
origin: "live logs, exact dependency regression, native source review and read-only probes"
created: "2026-09-12"
updated: "2026-09-12"
---

Live regression fixes (after bug_computer_review_lifetime_focus_error_media.md). Logs read-only: ~/.config/neoism/log/workspace-service.log around 2026-09-12T03:11:01 contains Enigo panic dumping compositor-reserialized xkb_keycodes { ... } without section name. Exact root: generated map WAS valid libxkbcommon, but Enigo0.6.1 Keymap2::new_from_fd -> ParsedKeymap -> Keycodes::parse calls parse_section(...).unwrap() at parse_keymap.rs278; parser requires quoted name. Text itself bypasses Enigo, but its temporary map is then received by a later Enigo action (e.g. URL followed by Enter). Prior libxkbcommon-only tests could not catch this.

linux_text.rs now names all sections neoism_text and compiles/serializes with libxkbcommon BEFORE publishing. Compiled generated source and compositor reserialization both pass exact dependency FD loader/parser. scripts/check-computer-keymap.py uses cargo metadata --locked --offline to locate actual Enigo0.6.1 source; disposable crate imports unmodified keymap2/mod.rs incl private parser, default_keymap etc (not a copied/mock parser). Tests create real anonymous FD via production keymap_file, seek EOF, invoke Keymap2::new_from_fd and its native compile/parser route. Old unnamed map reproduces panic; fixed Google URL https://www.google.com/search?q=neoism+browser, uppercase, punctuation, Unicode repeats, tabs/newlines,512 chars pass. 12 tests including dependency parser tests pass. No desktop connection.

Text snapshots original compositor keymap via shortcuts::snapshot_keymap, restores with compositor ACK before destroying virtual keyboard. RAII tracks pending key, original FD, proxy/connection; error/cancel/unwind best-effort key-up/modifier reset/map restore/destroy/flush, no blocking Drop roundtrip. Restores subsequent shortcut capability instead of leaving only text symbols. Native restoration still needs authorized interactive validation.

SERIAL ownership moved into run_worker so test backends also exercise actual owner. serialized holds guard OUTSIDE native catch_unwind; unit-lock poison recovery invalidates FRAME and clear_poison; WouldBlock still genuine busy. native_boundary per batch action preserves fail-first progress and optional recovery image after panic; FRAME invalidated. Explicit chord release catches each release panic and continues remaining modifiers. Button Drop catches release unwind; Enigo automatic release_keys_when_dropped disabled to avoid redundant destructor re-entry/double-panic. Tests native panic->cleanup->next worker succeeds incl authorized async input panic then screenshot backend, local poisoned mutex recovery vs genuine busy; primary key panic+release panic still releases remaining modifier. Real blocked OS calls cannot be preempted and retain lock; panic=abort release profile cannot be caught (documented, no global profile change).

Hyprland focus verified dialect cause: installed0.56.2 commit efb5099 Lua config, while old payload legacy dispatch focuswindow address:... parses as invalid Lua. Read-only /eval return probe chooses Lua vs legacy; payload /dispatch hl.dsp.focus({ window = "address:0x..." }) or /dispatch focuswindow address:0x...; only validated hex IDs interpolated. Exact source inspected HyprCtl.cpp dispatchRequest: Lua wraps return hl.dispatch(expr), legacy parses dispatcher name. /eval return type(hl) replied ok; read-only focus CONSTRUCTOR with existing target (never invoked/dispatched) returned nonnil userdata. Focus never executed. Reply checker requires exact trimmed ok, reports bounded real error reply. Tests dialect responses, payload escaping/injection rejection, ack/error preservation.

Screenshot and batch schema now settle_ms0..3000 default1200. capture_settled samples display ~100ms using64x64 RGB,>=300ms observation +250ms quiet, <=0.5% channels diff>12 ignored (cursor), reference is quiet-period anchor to detect cumulative drift. Poll sleeps check cancellation<=25ms. Return latest actual frame with settling.settled/timedOut/elapsedMs/captures, not page readiness. Timeout returns image explicitly unsettled,0 immediate. Enforce topology unchanged, existing target/frame and error-image invariants. Tests animations/slow drift/cursor, budget boundaries, cancel prevents followup capture, batch panic recovery image remains error.

Integrated cargo check -p neoism-agent-server -p neoism passes; computer suite34passed/1live probe ignored; exact-source macOSARM64/WindowsMSVC crosschecks pass; MCP18 and model14 pass. Existing warnings remain. Docs updated. No typing/click/focus/screenshot executed; only logs/source and read-only Hyprland version/mode/constructor probes. Preserve concurrent unrelated edits, no release build.
