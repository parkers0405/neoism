---
name: "Computer-use admission race and Shift chords — FIXED"
description: "P1 admission-generation race fixed + zero-input barrier test; P2 shortcuts resolve unmodified native codes before modifiers; 14 tests and cross-checks pass"
type: "bug"
scope: "project"
origin: "independent review follow-up"
created: "2026-09-12"
updated: "2026-09-12"
---

Follow-up to feature_builtin_computer_use_mcp.md: independent P1/P2 review fixes implemented. P1: mcp::call_tool_in_session now calls computer_use::admit before reading current config, capturing STOP generation before enablement. BuiltinMcpService::call_tool_authorized_async gained Option<u64> revocation_generation parameter; computer service requires it, passes exact epoch into shared run_worker entry point. run_worker constructs Check from supplied epoch, NEVER samples new STOP for admission. Deterministic Barrier/thread test pauses after enabled read, disables/increments STOP before worker creation, invokes production run_worker with counted fake backend and asserts zero inputs. Tests must not assert initial Check is_ok without isolation: concurrent revoke test can increment global STOP.
P2: new computer_use/shortcuts.rs resolves complete native chord (modifiers+main) before presses; emits same native codes down and reverse up with RAII cleanup. Linux reads bounded compositor wl_keyboard keymap (xkbcommon0.9), resolves base-level symbols ignoring Shift/Caps/Control while retaining known layout group, sends Enigo raw XKB codes (Enigo subtracts8 on wire). No Key::Unicode remapping after Shift. Tests use real XKB US map to verify Shift+A->A, Shift+1->!, Ctrl+Shift+A->A, verify identical emitted down/up rawcodes/cancellation cleanup, and DE vsUS Y mapping. Reject missing base keys before modifiers. Reject ambiguous multiseat desktops; with multiple layout groups require compositor group information (unfocused clients may not receive it), never guess. Mac resolves ASCII via unmodified Carbon UCKeyTranslate and emits raw CGKeyCode; no Enigo fallback to keycode0 for missing symbols. Carbon ABI checked against Apple SDK: OptionBits=u32, UniCharCount=unsigned long/usize on target64bit. CFRelease only nonnull source. Windows keeps stable native VK keys, rejects Unicode shortcut fallback; literal Unicode text remains separate.
Verified final cargo test -p neoism-agent-server computer_use --lib =14 passed; mcp::tests=18 passed; cargo check -p neoism-agent-server -p neoism-agent-service-api passed; exact-source isolated harness cross-checks macOSARM64 + WindowsMSVC passed; git diff --check passed. Pre-existing caller.rs unused import warning only. No live desktop actions/OS runtime verification. Existing single-output Wayland pointer limitation, non-preemptible native calls remain. Updated docs/computer-use.md; preserved unrelated edits.
