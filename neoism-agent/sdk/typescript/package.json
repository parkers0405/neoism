{
  "name": "@neoism/sdk-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "contract:print": "cargo run --quiet --manifest-path ../../../Cargo.toml -p neoism-agent -- openapi",
    "contract:check": "bash ../../scripts/openapi.sh check",
    "contract:update": "bash ../../scripts/openapi.sh update",
    "test": "npm run build && node ../../scripts/generate-contract.test.mjs && node scripts/plugin-host.test.mjs && node scripts/sdk.test.mjs",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.examples.json --pretty false",
    "gui:dev": "npm run dev -w @neoism/gui",
    "gui:build": "npm run build && npm run build -w @neoism/gui",
    "gui:test": "npm run test -w @neoism/gui && npm run test:assets -w @neoism/gui"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.5.0"
  }
}
