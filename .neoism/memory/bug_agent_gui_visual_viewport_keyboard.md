---
name: "Agent TS GUI visual viewport keyboard layout — fixed"
description: "Standalone Agent GUI VisualViewport keyboard/focus-pan layout fix, tests, deployment and simulated Chromium verification"
type: "bug"
scope: "project"
origin: "neoism-agent"
created: "2026-09-17"
updated: "2026-09-17"
---

Implemented in standalone Agent TS GUI (`neoism-agent/sdk/typescript/packages/gui`) without Rust changes/restarts. `App.tsx` now rAF-batches VisualViewport resize+scroll and window/focus events, tracks height/width/offsetTop/offsetLeft, pins `.app` with fixed top/left/width/height (no transform containing block), ignores pinch (`scale != 1`), classifies keyboard only for focused composer editable + >=120px/18% height reduction, retains state through blur-before-close ordering, clears when viewport restores even if focus remains, resets baseline on rotation, cleans listeners/frame/style vars, and includes viewport offsets in home-to-chat animation veto. CSS contains html/body/#root overflow, uses absolute app-relative mobile drawer/scrim/FX, and mobile keyboard mode bottom-docks home composer while hiding wordmark/footer hints; chat dock remains its existing absolute/safe-area layout; textarea remains 16px. Tests added in App.layout.test.tsx for resize/scroll batching, offset, focus/blur close, pinch, toolbar reduction, rotation, cleanup, containment, home/chat/desktop invariants. Verification: focused 37 tests pass; `npx vitest run src` 700/700 pass; production `npm run build` pass; package staging tests 4/4; diff check pass. Plain `npm test` has a pre-existing runner-glob problem: Vitest discovers Node's scripts/check-module-paths.test.mjs and says no Vitest suite, though its 3 node:test cases and all 700 Vitest cases pass; build runs that Node test correctly. Built GUI staged atomically using `node scripts/package-agent-gui.mjs .../dist ~/.local/bin/web/agent-gui`; :4096 serves matching `index-BHxTTrWO.js`, no restart. Current live Vite debug URL discovered via ss is http://127.0.0.1:5175 (not older 5176 hint). Isolated headless Chromium/CDP checked actual CSS geometry at 390x844, simulated keyboard via viewport shrink to 390x444 (not real device keyboard): keyboard class true, app 390x444, home dock bottom 432 vs app 444, footer hidden, no document overflow; restoration with textarea still focused clears class; pageScaleFactor 2 leaves app layout 390x844 and keyboard false; desktop 1280x800 no root overflow. No actual phone validation claimed.
