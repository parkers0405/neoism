// POD <-> backend translators collected from across the desktop binary.
// Each submodule converts between desktop-only types and the shared
// `neoism-ui` / backend representations.
pub mod island;
pub mod utils;
